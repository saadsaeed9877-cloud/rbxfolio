{
  "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @rbxfolio/database generate && pnpm build --filter=@rbxfolio/web",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs"
}
